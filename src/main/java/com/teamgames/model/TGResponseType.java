package com.teamgames.model;

public enum TGResponseType {
    JOIN,
    FLAG,
    MARK_OPPONENT,
    MARK_SELF,
    DEPLOY,
    ADMIN,
    PLAYERS,
    TEAM_SPLIT_DONE,
    START_GAME,
    END_GAME
}
